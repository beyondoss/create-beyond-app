# Git refs for the Beyond primitive repos the harness clones + builds.
# Pin to a tag/commit for reproducible builds; override per-run, e.g.
#   AUTH_REF=v1.2.3 pnpm e2e
AUTH_REF=main
KV_REF=main
QUEUE_REF=main
OBJECTS_REF=main
